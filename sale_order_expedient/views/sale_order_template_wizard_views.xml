<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Comment out or remove the problematic view since the model doesn't exist -->
    <!--
    <record id="view_sale_order_template_wizard_form" model="ir.ui.view">
        <field name="name">sale.order.template.wizard.form</field>
        <field name="model">sale.order.template.wizard</field>
        <field name="arch" type="xml">
            <form string="Create Quotation from Template">
                <group>
                    <field name="template_id" readonly="1"/>
                    <field name="partner_id" options="{'no_create': True}"/>
                </group>
                <footer>
                    <button name="action_create_quotation" string="Create" type="object" class="btn-primary" data-hotkey="q"/>
                    <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="z"/>
                </footer>
            </form>
        </field>
    </record>
    -->

    <!-- Redefine the action to use the existing expedient.create.wizard model -->
    <record id="action_sale_order_template_wizard" model="ir.actions.act_window">
        <field name="name">Create Expedient from Template</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">expedient.create.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="sale_management.model_sale_order_template"/>
        <field name="binding_view_types">form</field>
    </record>
</odoo>
